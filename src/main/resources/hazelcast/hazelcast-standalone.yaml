hazelcast:
  import:
    - classpath:hazelcast/hazelcast.yaml
  network:
    join:
      auto-detection:
        enabled: false
      multicast:
        enabled: false
    rest-api:
      enabled: true