hazelcast:
  import:
    - classpath:hazelcast/hazelcast.yaml
  network:
    join:
      kubernetes:
        enabled: true
        namespace: default
        service-name: app-vultr-k8s-demo
    rest-api:
      enabled: true